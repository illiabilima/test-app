import { Tabs } from "./Tabs";
import "./Tabs.css";
import { ReactElement } from "react";

export default function Tabs({}: Tabs): ReactElement {
	return (
		<header className="header">

		</header>
	);
}